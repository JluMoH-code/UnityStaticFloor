using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ZombBehaviour : Behav
{
    // Start is called before the first frame update
    void Start()
    {
        /*conditions.Add(gameObject.AddComponent(typeof(CEnemyDied)) as CEnemyDied);
        conditions.Add(gameObject.AddComponent(typeof(CEnemyFound)) as CEnemyFound);
        conditions.Add(gameObject.AddComponent(typeof(CEnemyLost)) as CEnemyLost);
        conditions.Add(gameObject.AddComponent(typeof(CEnemyStrong)) as CEnemyStrong);
        conditions.Add(gameObject.AddComponent(typeof(CHelpFound)) as CHelpFound);
        conditions.Add(gameObject.AddComponent(typeof(CPathEnd)) as CPathEnd);
        conditions.Add(gameObject.AddComponent(typeof(CTimeOut)) as CTimeOut);
        conditions.Add(gameObject.AddComponent(typeof(CWeaponDetect)) as CWeaponDetect);

        states.Add(gameObject.AddComponent<SAttack>());
        states.Add(gameObject.AddComponent<SPatrol>());
        states.Add(gameObject.AddComponent(typeof(SRun)) as SRun);
        states.Add(gameObject.AddComponent(typeof(SSearch)) as SSearch);
        states.Add(gameObject.AddComponent(typeof(SWaiting)) as SWaiting);*/
    }
}
